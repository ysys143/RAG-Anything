# EZIS RAG - WeDataLab
# Editable install of RAG-Anything from local upstream
-e ./upstream[all]

# LightRAG core with API server (pinned version)
lightrag-hku[api]==1.4.9.8

# PostgreSQL backends
psycopg2-binary>=2.9.0
asyncpg>=0.29.0

# Development tools
python-dotenv>=1.0.0
